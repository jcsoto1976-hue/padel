# Entorno del servidor
NODE_ENV=development
PORT=4000
TZ=Europe/Madrid

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=padel_club
DB_USER=padel_user
DB_PASSWORD=padel_pass_2024

# JWT
JWT_SECRET=padel_super_secret_jwt_key_2024_change_in_production
JWT_EXPIRES_IN=7d

# Frontend URL (para CORS)
CLIENT_URL=http://localhost:5173
