{
  "name": "padel-club-root",
  "version": "1.0.0",
  "description": "Orquestador raíz para ejecutar el frontend y backend del Club de Pádel",
  "scripts": {
    "dev": "start cmd /k \"npm run dev --prefix backend\" && npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  }
}
